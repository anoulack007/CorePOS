meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/stores/{{storeId}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "items": [
      {
        "product_id": "{{productId}}",
        "quantity": 1
      }
    ]
  }
}
